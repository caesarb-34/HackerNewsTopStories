// This enum is used for any spinner state
export enum RequestState {
  PRISTINE = 'pristine',
  WAITING = 'waiting',
  SUCCESS = 'success',
  FAIL = 'fail'
}

export enum ContentType {
  SECTION = 'section',
  ELEMENT = 'element',
  HELP = 'help',
  MODAL = 'modal',
  QUESTION = 'question',
  QUESTION_GROUP = 'question-group'
}
